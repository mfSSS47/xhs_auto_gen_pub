# 小红书自动化系统 — 完整操作指南

> 本文档面向**实际操作**，按场景编排，每一步都有可复制执行的命令。

---

## 目录

1. [环境准备](#1-环境准备)
2. [账号配置](#2-账号配置)
3. [单篇帖子发布（核心流程）](#3-单篇帖子发布核心流程)
4. [连续发布多篇帖子](#4-连续发布多篇帖子)
5. [仅生成内容（不发布）](#5-仅生成内容不发布)
6. [Web UI 使用](#6-web-ui-使用)
7. [帖子定义模板](#7-帖子定义模板)
8. [日常维护](#8-日常维护)
9. [故障排查](#9-故障排查)
10. [常用速查表](#10-常用速查表)

---

## 1. 环境准备

### 1.1 安装依赖

```bash
cd D:\TRAE项目\xhs_demo
pip install -r requirements.txt
playwright install chromium
```

### 1.2 验证环境

```bash
python -c "from models.database import init_db; init_db(); print('OK: 数据库已初始化')"
python -c "from playwright.sync_api import sync_playwright; print('OK: Playwright 可用')"
```

### 1.3 编码设置（Windows 必做）

在终端执行前设置编码，避免 emoji 乱码：

```bash
# Git Bash
export PYTHONIOENCODING=utf-8

# PowerShell
$env:PYTHONIOENCODING="utf-8"

# CMD
set PYTHONIOENCODING=utf-8
```

---

## 2. 账号配置

### 2.1 获取 Cookie

1. 浏览器打开 https://creator.xiaohongshu.com 并登录
2. 按 `F12` 打开开发者工具 → `Application` → `Cookies` → `creator.xiaohongshu.com`
3. 将所有 Cookie 拼接成字符串：`key1=value1; key2=value2; ...`

关键 Cookie 项（必须包含）：
- `web_session`
- `id_token`
- `a1`
- `customerClientId`
- `x-user-id-creator.xiaohongshu.com`
- `access-token-creator.xiaohongshu.com`
- `galaxy.creator.beaker.session.id`

### 2.2 添加/更新账号

```bash
# 首次添加
python main.py account-add --credential "你的完整cookie字符串" -n "我的小红书"

# 查看已添加的账号
python main.py account-list

# 验证账号是否有效
python main.py account-verify --account-id <账号ID>
```

### 2.3 Cookie 更新（过期后）

```bash
python -c "
from models.database import SessionLocal
import models.user
from models.user import Account
from core.security import encrypt_credential
db = SessionLocal()
acct = db.query(Account).filter(Account.platform=='xhs', Account.status=='active').first()
acct.credential = encrypt_credential('你的新cookie字符串')
db.commit()
print(f'Updated: {acct.id} ({acct.nickname})')
db.close()
"
```

### 2.4 多账号切换

```bash
# 列出所有账号，记下目标 ID
python main.py account-list

# 将目标账号设为 active，其他设为 inactive
python -c "
from models.database import SessionLocal
import models.user
from models.user import Account
db = SessionLocal()
# 全部设为 inactive
db.query(Account).filter(Account.platform=='xhs').update({'status':'inactive'})
# 激活指定账号
acct = db.query(Account).filter(Account.id=='<目标ID>').first()
acct.status = 'active'
db.commit()
print(f'Activated: {acct.id} ({acct.nickname})')
db.close()
"
```

---

## 3. 单篇帖子发布（核心流程）

这是最常用的操作。整个过程分 **6 个步骤**，你和 Claude 配合完成。

> **重要**：管道结束后会自动清理 `_claude_*.json` 文件。如果管道异常中断，手动运行 `python main.py cleanup` 即可恢复干净状态。

### Step 1：确保干净环境

```bash
python main.py cleanup
```

这会清理残留的 `_claude_*.json` 文件，并杀掉任何僵尸管道进程。

### Step 2：确认账号可用

```bash
python main.py account-list
# 确认有 status=active 的账号
```

### Step 3：启动管道

```bash
export PYTHONIOENCODING=utf-8
python main.py generate -t "你的主题" --publish
```

程序会输出：
```
[Stage 1/4] Content direction created: xxxxxxxxxxxx
>>> 请在 Claude 对话中发送: go-gen
```

此时管道进入等待状态，每秒检测一次 `_claude_response.json`。

### Step 4：生成内容（你在 Claude 对话中操作）

在对话中发送 **`go-gen`**，Claude 会自动读取请求文件、生成帖子、写入响应。

发送后 Claude 生成的帖子将包含：

### Step 5：审核内容（管道自动触发）

管道读取生成结果后，立即进入审核阶段，终端显示：
```
>>> 请在 Claude 对话中发送: go-review
```

在对话中发送 **`go-review`**，Claude 会自动审核并写入结果。

### Step 6：自动发布

审核通过后，管道自动进入发布阶段：
- Playwright 启动无头 Chromium
- 注入 Cookie 登录
- 上传图片、填写标题正文、添加话题
- 调用发布按钮
- 等待成功信号（"发布成功" / URL 跳转）

### Step 7：验证结果

查看管道最终输出：
```
[Stage 4/4] Published: platform_id=published_via_browser
=== Pipeline completed successfully ===
```

然后在浏览器中验证：
1. 打开 https://creator.xiaohongshu.com/note/manage
2. 查看笔记列表，确认新帖子出现

或者用命令查看数据库状态：

```bash
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import Post
db = SessionLocal()
p = db.query(Post).order_by(Post.created_at.desc()).first()
print(f'ID: {p.id}')
print(f'标题: {p.title}')
print(f'状态: {p.status}')
print(f'正文前100字: {(p.body or \"\")[:100]}')
print(f'话题: {p.hashtags}')
db.close()
"
```

---

## 4. 连续发布多篇帖子

### 4.1 推荐方式：逐篇发布

每篇帖子独立操作，前一篇完全结束后再开始下一篇：

```
循环发布流程：
┌─────────────────────────────────────────────┐
│  1. rm -f _claude_*.json                     │
│  2. python main.py generate -t "主题N" --publish │
│  3. Claude 响应两次（生成 + 审核）              │
│  4. 等待管道完成                              │
│  5. 检查数据库确认 published                   │
│  6. 回到步骤 1，发布下一篇                     │
└─────────────────────────────────────────────┘
```

### 4.2 实际操作示例

```bash
# 第一篇
python main.py cleanup
export PYTHONIOENCODING=utf-8
python main.py generate -t "如何做到健康饮食" --publish
# ... 在 Claude 对话中响应生成和审核请求 ...
# 管道结束后自动清理 _claude_*.json

# 确认第一篇成功
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import Post
db = SessionLocal()
p = db.query(Post).order_by(Post.created_at.desc()).first()
print(f'{p.id}: [{p.status}] {p.title}')
db.close()
"

# 第二篇（确认第一篇 published 后执行）
python main.py cleanup
python main.py generate -t "下一个主题" --publish
# ... Claude 响应 ...
```

### 4.3 注意事项

| 注意点 | 说明 |
|--------|------|
| **间隔** | 内置限流器默认 5条/分钟，连续发布会自动间隔 12s |
| **Cookie** | 每次发布前都会认证 Cookie，不需要手动刷新 |
| **残留文件** | 管道结束自动清理；也可手动 `python main.py cleanup` |
| **重复方向** | 同一主题多次运行会创建多个 ContentDirection，不影响发布 |

---

## 5. 仅生成内容（不发布）

适合先预览、修改再手动发布的场景。

```bash
export PYTHONIOENCODING=utf-8
python main.py gen-only -t "你的主题" --tone casual
```

管道只执行 Stage 1-2（创建方向 + 生成内容），跳过审核和发布。生成后需要 Claude 响应一次（生成内容）。

生成结果存储在数据库中，状态为 `pending_review`。后续可以手动发布：

```bash
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import Post
from services.review_service import review_service
from services.publish_service import publish_service
import asyncio

db = SessionLocal()
# 找到最新未发布的帖子
post = db.query(Post).filter(Post.status == 'pending_review').order_by(Post.created_at.desc()).first()
if post:
    print(f'Publishing: {post.title}')
    review_service.approve_manually(db, post, reviewer='human')
    db.refresh(post)
    record = asyncio.get_event_loop().run_until_complete(
        publish_service.publish_post(db=db, post=post)
    )
    print(f'Result: {record.status}, platform_id={record.platform_post_id}')
else:
    print('No pending posts found')
db.close()
"
```

---

## 6. Web UI 使用

Web UI 提供浏览器端的表单操作，适合快速原型和批量草稿生成。

### 6.1 启动

```bash
cd D:\TRAE项目\xhs_demo
export PYTHONIOENCODING=utf-8
python run_web.py
```

打开浏览器访问 `http://127.0.0.1:5000`。

### 6.2 操作流程

1. **首页** — 填写帖子标题和内容要求，上传图片（可选）
2. **提交** — 点击提交后创建 Job，后台 worker 自动处理
3. **查看状态** — 页面自动跳转到 `/status/<job_id>`，每 3 秒轮询进度
4. **完成** — 生成、审核、发布全自动完成，显示结果

### 6.3 Web UI 与 CLI 的区别

| 对比 | CLI（main.py） | Web UI（run_web.py） |
|------|---------------|---------------------|
| AI 生成 | Claude（文件协议，高质量） | DirectGenerator（本地规则，快速） |
| 需要 Claude 参与 | 是（需要响应两次） | 否（完全自动化） |
| 适用场景 | 精编高质量内容 | 快速草稿、批量生成 |

### 6.4 查看任务列表

浏览器访问 `http://127.0.0.1:5000/jobs` 查看最近 30 个任务。

---

## 7. 帖子定义模板

在请求 Claude 生成帖子时，使用以下模板提供完整需求（直接在对话中告诉 Claude）：

```markdown
## 帖子定义：<一句话主题>

| 属性 | 值 |
|------|-----|
| 主题 | <核心主题> |
| 风格 | casual / professional / storytelling / tutorial |
| 目标受众 | <如：25-35岁上班族女性> |
| 关键词 | <3-5个核心词，逗号分隔> |

### 核心信息要点
1. <信息点 1>
2. <信息点 2>
3. <信息点 3>

### 要求
- 标题：≤ 20 字（硬性限制），吸引眼球
- 正文：300-800 字，分段清晰，适当使用 emoji
- 话题：3-5 个相关标签
- 结尾：引导互动（提问/号召评论）

### 参考风格（可选）
<可粘贴一篇喜欢的小红书帖子>
```

### 示例：完整的帖子定义

```markdown
## 帖子定义：新手健身入门避坑指南

| 属性 | 值 |
|------|-----|
| 主题 | 新手健身入门避坑指南 |
| 风格 | casual |
| 目标受众 | 刚开始健身的20-30岁上班族 |
| 关键词 | 新手健身, 增肌, 避坑, 私教 |

### 核心信息要点
1. 健身第一周最容易犯的 3 个错误
2. 如何选择适合自己的训练计划
3. 饮食搭配的基本原则

### 要求
- 标题：≤ 20 字，有吸引力
- 正文：300-800 字
- 话题：3-5 个
- 结尾：引导读者评论自己的健身经历
```

---

## 8. 日常维护

### 7.1 查看数据库状态

```bash
# 查看所有内容方向
python main.py directions

# 查看最近 5 篇帖子
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import Post
db = SessionLocal()
for p in db.query(Post).order_by(Post.created_at.desc()).limit(5).all():
    print(f'{p.id} | {p.status:12} | {p.title}')
db.close()
"

# 查看最近发布记录
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import PublishRecord
db = SessionLocal()
for r in db.query(PublishRecord).order_by(PublishRecord.created_at.desc()).limit(5).all():
    print(f'{r.id} | {r.status:12} | platform={r.platform_post_id} | {r.error_msg or \"-\"}')
db.close()
"
```

### 7.2 清理残留

```bash
# 一键清理（推荐）
python main.py cleanup

# 等价的手动操作
rm -f _claude_request.json _claude_response.json
```

### 7.3 处理失败的帖子

如果帖子状态卡在 `publishing` 或 `failed`：

```bash
# 手动重置状态
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import Post
db = SessionLocal()
# 将所有 publishing 状态的帖子重置为 failed
db.query(Post).filter(Post.status == 'publishing').update({'status': 'failed'})
db.commit()
print('Done')
db.close()
"
```

然后可以重新对该帖子执行发布（见 5 节末尾的手动发布命令）。

### 7.4 查看日志

```bash
# 查看今天的日志
tail -100 logs/app.log

# 查找错误
grep -i "error\|failed\|exception" logs/app.log | tail -20
```

---

## 9. 故障排查

### 8.1 Cookie 过期

**症状**：管道输出 `Redirected to login!` 或 `PlatformAuthError`

**解决**：
```bash
# 1. 重新获取 Cookie（浏览器登录 creator.xiaohongshu.com → F12 → Cookies）
# 2. 更新数据库
python -c "
from models.database import SessionLocal
import models.user
from models.user import Account
from core.security import encrypt_credential
db = SessionLocal()
acct = db.query(Account).filter(Account.platform=='xhs', Account.status=='active').first()
acct.credential = encrypt_credential('新cookie字符串')
db.commit()
print('Cookie updated')
db.close()
"
```

### 8.2 标题过长

**症状**：`_onPublish` 返回 `true` 但页面出现 toast "标题最多输入20字哦~"，帖子未真正发布

**解决**：已自动修复。`xhs_adapter.py` 会自动截断标题到 20 字，`validators.py` 也会在审核阶段拦截。

### 8.3 管道卡在等待响应

**症状**：终端一直显示 "等待 Claude 响应"，没有进展

**解决**：
```bash
# 1. 检查是否写了响应文件
cat _claude_request.json

# 2. 如果没有生成响应，手动写入 _claude_response.json（见 6.2 节的格式）

# 3. 如果管道进程已死，重新启动
rm -f _claude_request.json _claude_response.json
python main.py generate -t "主题" --publish
```

### 8.4 发布后看不到帖子

**症状**：管道显示成功，但小红书创作者中心看不到

**可能原因**：
1. 帖子正在审核中（XHS 平台审核，需要几分钟到几小时）
2. Cookie 指向了不同的账号
3. 发布的是占位图帖子（没有提供真实 image_urls）

**排查**：
```bash
# 检查发布记录中的 platform_post_id
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import PublishRecord
db = SessionLocal()
r = db.query(PublishRecord).order_by(PublishRecord.created_at.desc()).first()
print(f'status={r.status}, platform_post_id={r.platform_post_id}, error={r.error_msg}')
db.close()
"
```

### 8.5 Playwright 报错

**症状**：`playwright._impl._errors.Error` 或浏览器无法启动

**解决**：
```bash
# 重新安装 Chromium
playwright install chromium --force

# 检查系统依赖
playwright install-deps chromium
```

---

## 10. 常用速查表

### 10.1 命令速查

| 操作 | 命令 |
|------|------|
| 发布一篇帖子 | `python main.py generate -t "主题" --publish` |
| 仅生成不发布 | `python main.py gen-only -t "主题"` |
| 查看账号 | `python main.py account-list` |
| 查看方向历史 | `python main.py directions` |
| 清理残留 | `python main.py cleanup` |
| 启动 Web UI | `python run_web.py` |

### 9.2 状态码速查

| post.status | 含义 |
|-------------|------|
| `draft` | 草稿 |
| `pending_review` | 等待审核 |
| `approved` | 审核通过，等待发布 |
| `publishing` | 正在发布中 |
| `published` | 已成功发布 |
| `rejected` | 审核未通过 |
| `failed` | 发布失败 |

### 9.3 标题限制速查

| 平台 | 标题限制 | 处理方式 |
|------|---------|---------|
| 小红书 | **20 字符** | 自动截断 |
| 本系统 validator | ≤ 20 字符 | 审核阶段拦截 |

### 10.4 触发 Claude 处理

| 阶段 | 在对话中发送 |
|------|-------------|
| 生成内容 | `go-gen` |
| 审核内容 | `go-review` |

Claude 收到后会读取 `_claude_request.json` 并写入 `_claude_response.json`，管道自动继续。

---

## 附录：完整发布流程图

```
用户操作                          Claude 操作                   系统操作
────────                        ──────────                   ──────────
输入命令
python main.py
generate -t "X"
--publish
    │                                                         创建方向
    │                                                         ContentDirection
    │                                                         写入数据库
    │                              ┌──────────────────┐
    │  发送 "go-gen" ─────────────►│ 读取 request     │
    │                              │ 生成帖子内容      │
    │                              │ 写入 response    │
    │                              └──────────────────┘
    │                                                         读取 response
    │                                                         写入 Post
    │                              ┌──────────────────┐
    │  发送 "go-review" ──────────►│ 读取 request     │
    │                              │ 审核帖子内容      │
    │                              │ 写入 response    │
    │                              └──────────────────┘
    │                                                         读取 response
    │                                                         通过审核？
    │                                                          ├─ Yes → 继续
    │                                                          └─ No  → 终止
    │                                                         启动 Playwright
    │                                                         上传图片
    │                                                         填写内容
    │                                                         点击发布
    │                                                         检测成功信号
    │                                                         更新数据库
    ▼
查看结果
```

---

> **文档版本**：v2.0 | **最后更新**：2026-05-29
