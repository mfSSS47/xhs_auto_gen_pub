# 小红书自动化内容生成与发布系统 — 项目说明书

## 1. 项目概述

**项目名称**：xhs_demo（小红书自动化内容生成与发布系统）

**项目定位**：一套全自动化的 **小红书（xiaohongshu/RED）帖子生产流水线**，从内容方向设定 → Claude AI 生成帖子 → 质量审核 → Playwright 浏览器自动化发布到小红书平台，实现"一键创作 + 发布"闭环。

**运行方式**：命令行 CLI（Python），Claude 助手通过文件协议参与内容生成与审核。

---

## 2. 技术栈

| 类别 | 技术 | 用途 |
|------|------|------|
| 语言 | Python 3.10+ | `from __future__ import annotations` 启用延迟注解 |
| AI 创作 | Claude（文件协议） | 通过 `_claude_request.json` / `_claude_response.json` 与本对话 Claude 交互 |
| 平台交互 | Playwright（Chromium 自动化） | 浏览器自动化：上传图片、填写表单、点击发布 |
| 平台 API | `httpx>=0.25.0` + `xhshow` | 签名生成 + HTTP 调用创作者 API（认证/状态查询） |
| 数据库 | SQLite + SQLAlchemy 2.0 ORM | 持久化账号、内容方向、帖子、审核、发布记录 |
| 配置 | `pydantic-settings` + `.env` 文件 | 环境变量/配置文件加载 |
| 日志 | `loguru>=0.7.0` | 结构化日志，同时输出到控制台和文件 |
| 安全 | `cryptography>=41.0.0`（AES-256-GCM） | 平台凭证加密存储 |
| 依赖管理 | `requirements.txt` | 项目依赖声明 |

---

## 3. 目录结构

```
xhs_demo/
├── main.py                    # CLI 入口，定义子命令和参数解析
├── run_web.py                 # Web UI 入口（Flask 应用）
├── requirements.txt           # Python 依赖清单
├── .env                       # 环境配置（API URL、发布参数等）
├── .encryption_key            # AES 密钥文件（自动生成）
├── xhs_demo.db                # SQLite 数据库文件
├── _claude_request.json       # Claude 适配器请求文件（临时）
├── _claude_response.json      # Claude 适配器响应文件（临时）
│
├── adapters/                  # 适配器层 — 外部系统对接
│   ├── ai/                    #    AI 适配器
│   │   ├── base.py            #      抽象基类 + 数据类（GeneratedPost, ReviewResult）
│   │   └── claude_adapter.py  #      Claude 文件通信实现（CLI 管道使用）
│   └── platform/              #    平台适配器
│       ├── base.py            #      抽象基类 + 数据类（AuthResult, PublishResult）
│       └── xhs_adapter.py     #      小红书适配器（Playwright 发布 + xhshow API 认证）
│
├── core/                      # 核心基础设施
│   ├── config.py              #    pydantic-settings 配置定义
│   ├── exceptions.py          #    自定义异常体系
│   └── security.py            #    凭证加密/解密（AES-256-GCM）
│
├── models/                    # 数据模型（SQLAlchemy ORM）
│   ├── database.py            #    引擎、Session、Base 声明
│   ├── content.py             #    ContentDirection（内容方向）
│   ├── post.py                #    Post（帖子）、ReviewRecord（审核）、PublishRecord（发布）
│   └── user.py                #    Account（平台账号）
│
├── services/                  # 业务逻辑层
│   ├── account_service.py     #    账号管理（CRUD + 凭证加解密）
│   ├── content_service.py     #    内容方向管理（CRUD + 验证）
│   ├── generation_service.py  #    内容生成编排
│   ├── review_service.py      #    质量审核编排（基础规则 + AI 审核）
│   ├── publish_service.py     #    发布编排（账号选择、重试、限流）
│   └── pipeline.py            #    全流程管道（Pipeline）
│
├── utils/                     # 工具层
│   ├── logger.py              #    loguru 配置（控制台 + 文件轮转）
│   └── validators.py          #    内容验证（敏感词、广告法、emoji 密度、XHS 标题长度限制）
│
├── web/                       # Web UI 层
│   ├── app.py                 #    Flask 应用工厂
│   ├── routes.py              #    Web 路由（页面 + JSON API）
│   ├── worker.py              #    后台管道工作线程（轮询 Job 队列）
│   ├── generator.py           #    DirectGenerator — 纯本地内容生成
│   ├── models.py              #    Job 模型（任务队列）
│   ├── templates/             #    Jinja2 模板
│   │   ├── base.html
│   │   ├── index.html         #       首页（表单提交）
│   │   ├── status.html        #       任务状态页
│   │   └── job_list.html      #       任务列表页
│   ├── static/                #    静态资源
│   │   ├── css/style.css
│   │   └── js/main.js
│   └── uploads/               #    用户上传图片（按 job 分目录）
│
└── logs/                      # 日志输出目录
```

---

## 4. 核心流程 — Pipeline

系统围绕 **4 阶段流水线（Pipeline）** 设计，所有阶段在 `services/pipeline.py` 的 `ContentPipeline` 类中编排：

```
  [Stage 1]                     [Stage 2]                     [Stage 3]                    [Stage 4]
  create_direction  ──────────►  generate  ──────────────────►  review  ──────────────────►  publish
  创建内容方向                    Claude 生成帖子                质量审核                      发布到小红书
                                │                               │
                                ▼                               ▼
                           数据库持久化                      审核记录入库
                           Post(status=draft)                ReviewRecord
```

### 4.1 Stage 1 — 创建内容方向（create_direction）
- 用户输入：主题(topic)、风格(tone)、目标受众、关键词、分类、字数限制、图片数量等
- 将 `ContentDirection` 记录写入数据库，状态为 `pending`
- 参数校验在 `utils/validators.py:validate_content_direction()`

### 4.2 Stage 2 — AI 生成内容（generate）
- `GenerationService` 调用 `ClaudeAdapter` 生成帖子
- ClaudeAdapter 将请求写入 `_claude_request.json`，等待 Claude 助手读取并生成内容
- Claude 将结果写入 `_claude_response.json`，适配器读取后返回
- 生成的 `Post` 持久化到数据库，状态为 `pending_review`
- 记录生成耗时和所用 AI 模型

### 4.3 Stage 3 — 质量审核（review）
- `ReviewService` 执行双重审核：
  - **基础规则检查**：空内容、长度、XHS 标题限制（20字）、emoji 密度、敏感内容、广告违禁词
  - **AI 审核**：ClaudeAdapter 对内容进行评分和问题诊断
- 得分 ≥ 6.0 且通过基础检查 → `approved`；否则 → `rejected`
- 审核记录写入 `ReviewRecord`

### 4.4 Stage 4 — 发布（publish）
- `PublishService` 将审核通过的帖子发布到小红书：
  - 解析账号凭证（AES-256-GCM 解密 Cookie）
  - xhshow 签名验证 Cookie 有效性
  - Playwright 浏览器自动化：导航到发布页 → 切换图文标签 → 上传图片 → 填写标题和正文 → 添加话题 → 点击发布
  - 带指数退避的自动重试（默认最多 3 次）
  - 内置速率限制（默认 5 条/分钟）
  - 标题过长自动截断至 20 字（XHS 平台限制）
- 发布记录写入 `PublishRecord`，帖子状态更新为 `published`

### 4.5 轻量模式：gen-only
跳过审核和发布阶段，仅生成内容并保存。适用于纯内容创作场景。

---

## 5. CLI 命令参考

通过 `python main.py <command> [options]` 执行：

| 命令 | 功能 | 必填参数 | 可选参数 |
|------|------|---------|---------|
| `generate` | 全流程：生成 → 审核 → 发布 | `-t/--topic` | `--tone`, `-a/--audience`, `-k/--keywords`, `-c/--category`, `--max-length`, `--images`, `-e/--extra`, `--publish`, `--account` |
| `gen-only` | 仅生成内容，不审核不发布 | `-t/--topic` | `--tone`, `-a/--audience`, `-k/--keywords`, `--max-length`, `-e/--extra` |
| `account-add` | 添加平台账号凭证 | `--credential` | `-n/--nickname`, `--platform` |
| `account-list` | 列出已添加的账号 | — | `--platform` |
| `account-verify` | 验证账号凭证有效性 | `--account-id` | — |
| `directions` | 查看内容方向历史 | — | — |
| `cleanup` | 清理残留文件 + 僵尸进程 | — | — |

### 风格（tone）选项
- `casual` — 轻松口语化，像闺蜜分享（默认）
- `professional` — 专业但不晦涩
- `storytelling` — 以个人经历故事形式展开
- `tutorial` — 步骤清晰的教学风格

---

## 6. AI 适配器

系统采用 **策略模式**，通过 `BaseAIAdapter` 抽象基类定义接口。提供两种实现：

| 适配器 | 位置 | 说明 |
|--------|------|------|
| **ClaudeAdapter** | `adapters/ai/claude_adapter.py` | 通过 JSON 文件协议与 Claude 通信，用于 CLI 管道 |
| **DirectGenerator** | `web/generator.py` | 纯本地规则引擎，用于 Web UI 独立运行 |

### 6.1 ClaudeAdapter 文件协议

ClaudeAdapter 不依赖任何外部 API，而是通过 JSON 文件与当前对话中的 Claude 助手通信：

```
┌──────────────┐         ┌───────────────────┐         ┌──────────────┐
│  Pipeline    │  写入    │  _claude_request   │  读取    │  Claude      │
│  (Python)    │ ──────► │  .json             │ ──────► │  (本对话)    │
│              │         └───────────────────┘         │              │
│              │         ┌───────────────────┐         │              │
│              │  读取    │  _claude_response  │  写入    │              │
│              │ ◄────── │  .json             │ ◄────── │              │
└──────────────┘         └───────────────────┘         └──────────────┘
```

**轮询机制**：适配器每秒检查 `_claude_response.json` 是否存在，最多等待 300 秒。读取后立即删除该文件，防止下次误读。

**安全性**：每次写请求前会主动删除旧的响应文件，避免读取到上一次遗留的响应。

### 6.2 JSON 协议格式

#### 生成请求（_claude_request.json）

```json
{
  "action": "generate_post",
  "topic": "如何做到健康饮食",
  "tone": "casual",
  "target_audience": "",
  "keywords": ["小白", "入门"],
  "extra_context": "",
  "max_length": 1000
}
```

#### 生成响应（_claude_response.json）

```json
{
  "title": "健康饮食入门指南",
  "body": "很多人以为健康饮食就是吃草...",
  "hashtags": ["健康饮食", "减脂餐", "生活化减脂"],
  "seo_keywords": ["健康饮食", "如何健康饮食", "减脂"]
}
```

#### 审核请求

```json
{
  "action": "review_content",
  "title": "健康饮食入门指南",
  "body": "很多人以为健康饮食就是吃草...",
  "hashtags": ["健康饮食", "减脂餐"]
}
```

#### 审核响应

```json
{
  "review_passed": true,
  "review_score": 9.0,
  "review_issues": [],
  "review_suggestion": "内容质量很好"
}
```

---

## 7. 平台适配器 — XHSAdapter

### 7.1 功能构成

XHSAdapter 承担两部分职责：

| 功能 | 技术 | 说明 |
|------|------|------|
| **认证** | httpx + xhshow 签名 | 调用 `edith.xiaohongshu.com/api/sns/web/v1/user/me`，使用 xhshow 库生成 x-s/x-t/x-s-common 签名头 |
| **发布** | Playwright（Chromium） | 无头浏览器自动化：导航到创作者中心 → 切换图文上传标签 → 上传图片 → 填写标题/正文/话题 → 调用 Vue 组件 `_onPublish()` 方法 |
| **状态查询** | httpx + xhshow 签名 | 调用 `/api/sns/web/v1/note/{id}` 查询帖子状态 |

### 7.2 发布流程（Playwright 详细步骤）

```
1. 启动 Chromium（无头模式，禁用 AutomationControlled 检测）
2. 注入 Cookie（从加密凭证解析）
3. 导航到 https://creator.xiaohongshu.com/publish/publish?from=menu
     ↓ （如果跳转到 login 页面 → 抛出 PlatformAuthError）
4. 等待 header-tabs 渲染 → 点击"上传图文"标签
5. 通过 input.upload-input 上传图片（支持多张）
6. 等待图片处理（约 12s）
7. 填写标题（input[placeholder*="标题"]）— 自动截断至 20 字
8. 点击 TipTap/ProseMirror 编辑器 → 键盘输入正文
9. 添加话题标签（通过 #topicBtn 面板）
10. 调用 xhs-publish-btn Vue 组件的 _onPublish() 方法
11. 轮询检测发布结果（"发布成功" 文本 / URL 变化，最多 90s）
12. 提取 note ID → 返回 PublishResult
```

### 7.3 注意事项

- **标题限制**：XHS 平台限制标题为 **20 个字符**（不是 30！）。adapter 会自动截断超长标题，validator 也会检查此限制。
- **图片**：如未提供 image_urls，自动生成 800×600 占位图。正式发布建议提供真实图片。
- **_onPublish()**：发布按钮是 Vue 自定义组件，内部 HTML 为空，必须调用其 `_onPublish()` 方法而不是 `.click()`。
- **话题选择**：通过 topic 搜索面板添加话题，搜索后选择第一个匹配项。

---

## 8. 帖子定义流程与模板

### 8.1 完整操作流程

当你要求 Claude 发布一篇帖子时，建议遵循以下流程：

```
第 1 步：确认环境就绪
├── 检查数据库中有 active 状态的 XHS 账号
├── 确认 Cookie 未过期（可先跑 account-verify）
└── 清理残留的 _claude_request.json / _claude_response.json

第 2 步：定义帖子内容
├── 确定主题（topic）、风格（tone）、关键词（keywords）
├── 如有特殊要求，通过 extra_context 或口头描述传给 Claude
└── 决定是否自动发布（--publish）

第 3 步：启动管道
└── python main.py generate -t "主题" --publish

第 4 步：Claude 生成内容
├── 管道写入 _claude_request.json
├── Claude 读取请求 → 生成标题（≤20字）、正文（≤1000字）、话题标签
└── Claude 将结果写入 _claude_response.json

第 5 步：Claude 审核内容
├── 管道读取生成结果 → 写入审核请求 _claude_request.json
├── Claude 读取帖子内容 → 审核评分
└── Claude 将审核结果写入 _claude_response.json

第 6 步：自动发布（如果 --publish）
├── Playwright 启动浏览器 → 登录小红书创作者中心
├── 上传图片、填写内容、添加话题
├── 点击发布 → 等待成功信号
└── 数据库记录更新为 published

第 7 步：验证发布
├── 检查管道输出日志（发布成功 / 失败）
├── 登录 https://creator.xiaohongshu.com/note/manage 查看笔记列表
└── 如失败，根据错误信息排查（常见：Cookie 过期、标题超长、网络问题）
```

### 8.2 帖子定义模板

在请求 Claude 生成小红书帖子时，可使用以下模板明确需求：

```markdown
## 帖子定义

**主题**：[一句话描述核心主题]
**风格**：[casual / professional / storytelling / tutorial]
**目标受众**：[目标读者画像，如：25-35岁上班族女性]
**关键词**：[3-5个核心关键词]

**核心信息要点**：
1. [信息点 1]
2. [信息点 2]
3. [信息点 3]

**语气要求**：
- [如：亲切自然，像朋友聊天]
- [如：多用短句，每句话不超过 25 字]

**特殊要求**：
- 标题控制在 20 字以内（XHS 硬性限制）
- 正文 300-800 字
- 3-5 个话题标签
- 结尾引导互动（提问/求关注）

**参考示例**（可选）：
[可粘贴一篇你喜欢的小红书帖子作为风格参考]
```

### 8.3 快速发布命令模板

```bash
# 标准发布（休闲风格，自动发布）
python main.py generate -t "你的主题" --publish

# 教程风格 + 指定关键词 + 自动发布
python main.py generate -t "你的主题" --tone tutorial -k "关键词1,关键词2" --publish

# 仅生成不发布（适合预览后手动发布）
python main.py gen-only -t "你的主题" --tone casual

# 更新 Cookie（当账号过期时）
python -c "
from models.database import SessionLocal
import models.user
from models.user import Account
from core.security import encrypt_credential
db = SessionLocal()
acct = db.query(Account).filter(Account.platform=='xhs', Account.status=='active').first()
acct.credential = encrypt_credential('你的新cookie字符串')
db.commit()
print('Cookie updated')
db.close()
"

# 查看所有帖子
python -c "
from models.database import SessionLocal
import models.content, models.post, models.user
from models.post import Post
db = SessionLocal()
for p in db.query(Post).order_by(Post.created_at.desc()).limit(5).all():
    print(f'{p.id}: [{p.status}] {p.title}')
db.close()
"
```

---

## 9. 安全设计

| 安全措施 | 实现位置 | 说明 |
|---------|---------|------|
| 凭证加密 | `core/security.py` | AES-256-GCM 加密存储平台 Cookie/Token |
| 密钥管理 | `core/security.py` | 自动生成 `.encryption_key`，也支持环境变量 `ENCRYPTION_KEY` |
| 敏感内容过滤 | `utils/validators.py` | 政治、色情、赌博、毒品、武器等敏感词检测 |
| 广告合规 | `utils/validators.py` | 《广告法》违禁词检测（"顶级""第一品牌""根治"等） |
| 内容验证 | `utils/validators.py` | 字数限制、emoji 密度控制（≤15%）、XHS 标题长度（≤20字） |
| 浏览器隐身 | `xhs_adapter.py` | 禁用 `AutomationControlled` 标记，伪装正常用户 |

---

## 10. 数据模型

### Account（账号）
- `id`, `platform`, `nickname`, `credential`（AES-256-GCM 加密）, `status`, `created_at`, `updated_at`

### ContentDirection（内容方向）
- `id`, `topic`, `target_audience`, `tone`, `keywords`(JSON), `category`, `max_length`, `image_count`, `extra_context`, `status`, `created_at`, `updated_at`

### Post（帖子）
- `id`, `direction_id`(FK), `account_id`(FK), `title`, `body`, `hashtags`(JSON), `image_urls`(JSON), `status`, `ai_model`, `generation_time_ms`, `created_at`, `updated_at`

### Post 状态机
```
draft → pending_review → approved → publishing → published
                           ↓                       ↓
                       rejected                  failed
```

### ReviewRecord（审核记录）
- `id`, `post_id`(FK), `result`(approved/rejected), `score`, `issues`(JSON), `reviewed_by`, `created_at`

### PublishRecord（发布记录）
- `id`, `post_id`(FK), `account_id`(FK), `platform_post_id`, `status`, `error_msg`, `retry_count`, `created_at`, `completed_at`

### Job（Web UI 任务）
- `id`, `title`, `requirements`, `image_paths`(JSON), `status`, `progress_message`, `error_message`, `direction_id`(FK), `post_id`(FK), `post_title`, `post_body`, `hashtags`(JSON), `review_score`, `review_issues`(JSON), `platform_post_id`, `created_at`, `updated_at`

---

## 11. 异常体系（`core/exceptions.py`）

```
AppException (base)
├── ValidationError          (1001) — 参数校验失败
├── NotFoundError            (1002) — 资源未找到
├── AIGenerationError        (2001) — AI 生成失败
├── AITimeoutError           (2002) — AI 生成超时
├── ReviewRejectedError      (3001) — 内容审核未通过
├── PlatformAuthError        (4001) — 平台认证失败
├── PublishError             (4002) — 发布失败
└── PublishTimeoutError      (4003) — 发布超时
```

---

## 12. 配置项（.env）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `XHS_API_BASE_URL` | `https://edith.xiaohongshu.com` | 小红书 API 端点 |
| `XHS_COOKIE` | — | 小红书登录 Cookie（备用） |
| `DATABASE_URL` | `sqlite:///xhs_demo.db` | 数据库连接串 |
| `LOG_LEVEL` | `INFO` | 日志级别 |
| `LOG_FILE` | `logs/app.log` | 日志文件路径 |
| `LOG_RETENTION` | `30 days` | 日志保留时间 |
| `PUBLISH_RETRY_MAX` | 3 | 发布最大重试次数 |
| `PUBLISH_RETRY_DELAY` | 2 | 重试初始延迟（秒），指数退避 |
| `PUBLISH_RATE_LIMIT_PER_MINUTE` | 5 | 每分钟最多发布数 |
| `UPLOAD_FOLDER` | `web/uploads` | Web UI 图片上传目录 |

---

## 13. 连续发布注意事项

### 13.1 已验证可正常工作的场景

- **同账号连续发布不同主题的帖子**：每次发布创建新的浏览器实例，互不干扰
- **发布成功后再发布新帖**：流程完全独立，前一帖不影响后续
- **失败重试后重新发布**：重试机制正确处理错误状态

### 13.2 已知限制与规避方法

| 问题 | 影响 | 规避方法 |
|------|------|---------|
| 管道中断后残留 `_claude_*.json` 文件 | 下次运行可能读取过期数据 | 管道启动前 `_write_request()` 会自动删除旧响应 |
| 重复运行同一主题产生多个 ContentDirection | 数据库出现重复方向 | 发布前先检查 `directions` 列表，删除不需要的旧方向 |
| Cookie 过期（通常 24h） | 认证失败，发布中断 | 发布前运行 `account-verify` 确认有效；过期后更新 Cookie |
| XHS 平台速率限制 | 发布过频可能触发风控 | 内置限流器（默认 5 条/分钟），可调整 `PUBLISH_RATE_LIMIT_PER_MINUTE` |
| 图片处理耗时波动 | 上传后等待时间不足可能导致表单验证失败 | 当前设置 12s 等待，网络慢时可适当增加 |
| Windows GBK 编码问题 | emoji/中文输出乱码 | 设置环境变量 `PYTHONIOENCODING=utf-8` |

### 13.3 推荐操作序列

连续发布多篇帖子时，推荐按以下顺序操作：

```bash
# 1. 发布前检查
python main.py account-list                  # 确认有 active 账号
rm -f _claude_request.json _claude_response.json  # 清理残留文件

# 2. 第一篇
python main.py generate -t "主题1" --publish  # 等待 Claude 响应两次（生成+审核）

# 3. 确认第一篇成功
python -c "..." # 检查数据库中的帖子状态

# 4. 清理残留文件
rm -f _claude_request.json _claude_response.json

# 5. 第二篇（如果第一篇成功）
python main.py generate -t "主题2" --publish

# 6. 重复 3-5 步

# 7. 最终验证
# 登录 https://creator.xiaohongshu.com/note/manage 确认所有帖子都在列表中
```

---

## 14. Web UI

系统提供了一个基于 Flask 的 Web 界面，可通过浏览器操作整个流程。

### 14.1 启动方式

```bash
python run_web.py
# 打开 http://127.0.0.1:5000
```

Web UI 适合"填写表单 → 提交任务 → 轮询状态"的交互场景，不需要与 Claude 对话交互。

### 14.2 与 CLI 管道的区别

| 对比点 | CLI 管道（main.py） | Web UI（run_web.py） |
|--------|---------------------|----------------------|
| AI 引擎 | **ClaudeAdapter** — 通过 JSON 文件与本对话 Claude 通信 | **DirectGenerator** — 纯本地规则引擎生成内容 |
| 交互方式 | 终端命令 + Claude 对话 | 浏览器表单 + API 轮询 |
| 审核方式 | Claude AI 审核 | 本地规则检查（标题长度、空内容） |
| 适用场景 | 高质量 AI 内容创作 | 快速原型、批量草稿生成 |

### 14.3 Web UI 架构

```
浏览器表单  ──POST──►  /api/jobs  ──写入──►  Job(queued)
                                                  │
                                         worker.py 每 2s 轮询
                                                  │
                                                  ▼
                                          1. 创建 Direction
                                          2. DirectGenerator 生成内容
                                          3. 本地规则审核
                                          4. Playwright 发布（如审核通过）
                                                  │
                                                  ▼
                                          Job 状态更新为 completed/failed
                                                  │
浏览器轮询  ◄──GET───  /api/jobs/<id>  ◄──读取──┘
```

### 14.4 DirectGenerator

`web/generator.py` 的 `DirectGenerator` 实现了 `BaseAIAdapter` 接口，但**不依赖 Claude**。它的工作原理：
- 解析用户输入的需求文本，提取要点
- 为每个要点生成结构化的段落（header → detail → tip）
- 从主题和需求文本中提取话题标签
- 进行基础的本地规则审核

这使 Web UI 可以独立运行，不需要等待 Claude 响应。

---

## 15. 设计亮点与扩展性

1. **适配器模式** — AI 适配器和平台适配器均基于抽象基类，可无缝切换或新增实现（如公众号、抖音等平台）
2. **Pipeline 流水线** — 4 阶段解耦，每个阶段可独立扩展、替换、跳过
3. **双重审核机制** — 规则引擎（硬性红线） + AI 评分（软性质量），兼顾安全与灵活性
4. **文件协议解耦** — ClaudeAdapter 通过 JSON 文件通信，Claude 无需运行在 Python 进程内，任何能读写文件的 Claude 实例均可参与
5. **安全优先** — 平台凭证全程 AES-256-GCM 加密，内置敏感内容和广告法合规检测
6. **速率控制** — 发布服务内置令牌桶式限流器，避免触达平台风控
7. **浏览器自动化** — Playwright 实现真实用户操作模拟，不依赖不稳定的私有 API
